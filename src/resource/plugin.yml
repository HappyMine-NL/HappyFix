# Plugin created by DeStilleGast

name: Fix
main: com.destillegast.Fix
version: "1.2"
commands:
  fix:
    permission: "fix.hand"
  fastfix:
    permission: "fix.hand"
  makeunrepairable:
    permission: "fix.create"

api-version: "1.13"
author: "DeStilleGast"
prefix: "Fix"